/** Shared classes for text inputs, selects and textareas. */
export const fieldClasses =
  'rounded-xl border border-line bg-surface-raised px-4 py-3 text-ink placeholder:text-ink-faint focus:border-accent focus:outline-none'

/** Variant for fields nested inside an already-raised card. */
export const fieldOnRaisedClasses =
  'rounded-xl border border-line bg-surface px-4 py-3 text-ink placeholder:text-ink-faint focus:border-accent focus:outline-none'

export const labelClasses = 'mb-1 block text-sm text-ink-muted'
