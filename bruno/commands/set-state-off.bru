meta {
  name: Set State OFF
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/devices/{{deviceId}}/command
  body: json
  auth: none
}

body:json {
  {
    "command": "setState",
    "payload": {
      "state": "OFF"
    }
  }
}
