meta {
  name: Set State ON
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/devices/{{deviceId}}/command
  body: json
  auth: none
}

body:json {
  {
    "command": "setState",
    "payload": {
      "state": "ON"
    }
  }
}
