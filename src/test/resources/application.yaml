quarkus:
  http:
    test-port: 0
  management:
    test-port: 0

# Disable MQTT channels in tests - consumers won't start
mp:
  messaging:
    incoming:
      z2m-devices:
        enabled: false
      z2m-bridge:
        enabled: false
      z2m-telemetry:
        enabled: false

influxdb:
  url: http://localhost:8086
  token: test-token
  org: smart-home
  bucket: telemetry